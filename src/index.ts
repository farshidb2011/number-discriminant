import {
    addDiscriminator,
    removeDiscriminator,
    InputError
} from './functions/number-discriminator';

export default {
    addDiscriminator,
    removeDiscriminator,
    InputError
}

export type { NumType } from './functions/number-discriminator';